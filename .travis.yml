language: python
python:
    - "2.6"
    - "2.7"
# command to install dependencies
install:
    - pip install Cython
    - pip install -r requirements.txt
# command to run tests
script:
    - nosetests --with-doctest && pylint --errors-only
branches:
    only:
        - master
